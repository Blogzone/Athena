<%- include('../includes/head.ejs') %>
    </head>
    <body>
        <div class="container">
            <%- include('../includes/nav.ejs') %>
                        <%- include('../includes/user-menu.ejs') %>
                                                
                    </ul>
                </nav>
            </header>
            
            <div class="content">
               <main class="my-blogs">
                <% if (blogs.length > 0) { %>
                    <h1 class="section-headline">Blogs on <%= pageTitle %></h1>
                    <% for (let i = blogs.length - 1; i>=0; i--) { %>
                        <div class="latest-blogs__blog">
                            <div class="latest-blogs__blogdetails">
                                <h2 class="latest-blogs__blog--title"><%= blogs[i].title %></h2>
                                <p class="latest-blogs__blog--info">Posted by <span style="font-weight: 600;"><%= blogs[i].userId.name %></span> on <%= blogs[i].publishedAt.toLocaleDateString() %> in <span style="font-weight:600;"><%= blogs[i].topic %></span></p>
                            </div>                        
                            <div class="latest-blogs__blogaction">
                                <a href="/blogs/<%= blogs[i]._id %>" class="btn">Read Blog</a>                                
                                
                            </div>                
                                                
                        </div>
                    <% } %>
                    
                    
                    
                <% } else { %>
                    <h1 class="section-headline">No Blogs yet in this Category..</h1>  
                <% } %>         
               </main>

               
            </div>
            

            <%- include('../includes/footer.ejs') %>
            
        </div>
        
    </body>
</html>